[project]
name = "flowforge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.1",
]

[project.scripts]
ff = "flowforge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
