[project]
name = "flowforge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.1",
    "langchain-ollama==1.1.0",
    "langgraph>=1.2.4",
]

[project.scripts]
ff = "flowforge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
